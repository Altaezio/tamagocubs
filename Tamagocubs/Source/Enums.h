#pragma once

// to split in multiple files in case of too many enums obviously

enum class CubState { idle, hungry, sick, wet, wontDo };

enum class TamActions { none, feed, heal, clean, dispute, doSport };
